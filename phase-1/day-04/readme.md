# Day 4 — Basic SEO Audit

Improvements applied across previous days:

- Added `<meta name="description">` to summarize content for search and link previews
- Added Open Graph tags (`og:title`, `og:description`) for richer social previews
- Ensured headings follow a logical hierarchy per page
- Confirmed all images include meaningful `alt` text
- Kept link text descriptive (no “click here”)

Files updated:

- `../day-01/index.html`: Description + OG tags for semantic article
- `../day-02/form.html`: Description + OG tags for forms page
-,`../day-03/table.html`: Description + OG tags for table page

Next steps (optional):

- Add `og:image` when you have a branded preview image
- Add canonical URL when deploying to a stable domain
- Validate metadata with the browser’s devtools and sharing debuggers

